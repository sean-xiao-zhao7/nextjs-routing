/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "seanxiao.com",
            },
        ],
    },
};

export default nextConfig;
